{
  "name": "velorci-site",
  "version": "2.0.0",
  "private": true,
  "description": "velorci marketing site — static, bilingual (ar/en), zero runtime dependencies",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
